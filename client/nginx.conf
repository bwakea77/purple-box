server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback: /r/<roomId> is client-side routed, not a real path.
  location / {
    try_files $uri /index.html;
  }
}
